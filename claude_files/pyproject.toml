[build-system]                     # tells pip how to build
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]                          # minimal metadata; expand later if you like
name = "nce"
version = "0.1"
