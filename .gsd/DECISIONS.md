# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Config translation boundary | `prepare_config()` in config_schema.py, not FastGM.__init__ | Clean separation — FastGM calls one function, gets flat dict | No |
| D002 | M001 | arch | Backward compat strategy | Auto-detect flat vs nested, pass flat through, translate nested | Existing scripts/notebooks must not break | No |
| D003 | M001 | arch | Internal config representation | Keep flat dict internally, translate at entry point only | Zero consumer code changes (bucket.py, train.py, etc.) | Yes — if full codebase migration justified |
| D004 | M001 | arch | State preservation module | New `nce/state/` module, separate from FastGM internals | User requested modular/separable design | No |
| D005 | M001 | arch | Default save mode | Training metadata only (loss curves, epochs, hidden sizes), no NN weights | Lightweight default; weights optional via flag | Yes — if weights become commonly needed |
| D006 | M001 | arch | Visualization module location | New `nce/visualization/` module (not extending nce/utils/plots.py) | Distinct capability deserves distinct module; existing plots.py is factor-comparison focused | No |
| D007 | M001 | convention | Config field name mapping | lr→learning_rate, ecl→exact_computation_limit, iB→i_bound, fdb→forward_diff_barrier | Readable names; old names accepted as aliases | No |
| D008 | M001 | arch | Loss curve capture | Extend per_bucket_training_log to include full loss curves before bucket deletion | Currently only label/epochs/hidden_sizes; loss curves live in FactorNN.losses which gets consumed | No |
| D009 | M002 | convention | Test framework | pytest in tests/ directory | Standard Python testing; extensible fixture/parametrize pattern | No |
| D010 | M001/S01 | convention | Dead field list corrected | Only `backward_ecl` and `num_batches_per_set` are dead; `loss_fn2`, `num_epochs2`, `complexity_limit`, `exact`, `memorizer` are LIVE | Code audit (S01-RESEARCH) found prior plan's dead-field list was wrong; these 5 fields have active consumers | No |
| D011 | M001/S01 | arch | Dead field handling: warn in flat, error in nested | Flat configs with dead fields get warning + strip; nested configs with dead fields get ValueError | Preserves backward compat (R005) for existing benchmark configs while enforcing clean new configs (R002) | Yes — can make both strict after S02 cleans benchmark configs |
| D012 | M001/S01 | arch | Decision tree fields folded into nn section | DT-specific fields (num_leaves, dt_lr, etc.) go in `nn` section with `dt_` prefix, not a 7th section | Keeps section count at 6; DT fields are an alternative to NN within the same section concept | Yes |
| D013 | M001/S01 | convention | `lower_dim` placed in sampling section | Despite affecting NN input representation, `lower_dim` controls sample encoding and lives in `sampling` | Consumed in sampling/preprocessing pipeline; prior plan's `output` placement was wrong | No |
| D014 | M001/S01 | arch | `prepare_config` defaults to `strict=False` | Dead fields warn+strip by default; `strict=True` raises ValueError | Benchmark configs contain dead fields — default must not break existing usage. Tests that want strict behavior pass `strict=True` explicitly. | Yes — flip to strict after S02 cleans benchmark configs |
| D015 | M001/S04 | arch | State dict is inspection-only, not reconstructable | `save_state`/`load_state` preserves training metadata and optional weights for inspection; does not reconstruct a live FastGM | Reconstruction requires the original model/UAI file — too complex and fragile for the use case; inspection data is what's needed | No |
| D016 | M001/S04 | arch | Weight capture happens at training time, stripping at save time | `save_nn_weights` config flag controls capture in bucket.py; `save_state(save_weights=False)` strips at serialization | Separates capture (must happen before bucket deletion) from persistence (user choice at save time); one inference run can produce both save modes | Yes |
| D017 | M001/S06 | arch | Training log format: JSONL | One JSON object per line (JSONL) via Python stdlib `logging` | Machine-parseable, grep-friendly, no schema overhead, no new dependencies | No |
| D018 | M001/S06 | arch | Logger namespace: `nce.training` | Dedicated namespace avoids collision with root logger suppression in stats.py | stats.py uses `logging.disable(logging.CRITICAL)` on root logger; `nce.training` namespace is unaffected | No |
| D019 | M001/S06 | convention | Logger module name: `training_logger.py` | Named `nce/training_logger.py` not `nce/logging.py` | Avoids shadowing Python's stdlib `logging` module | No |
| D020 | M002 | convention | Single-slice M002 | All 7 requirements (R018–R024) in one slice S01 | All tests share the same fixtures (hand-built factors + complete NN config), no inter-test dependencies, and the demoable unit is atomic (`pytest tests/` green). Splitting would create artificial boundaries. | Yes — split if scope grows |
| D021 | M002 | convention | Hand-built factors over catalog models for tests | Test problems constructed from `FastFactor` with known Z values, not loaded from model catalog | Catalog models require network/disk I/O, have non-deterministic availability, and don't have easily verifiable exact Z values. Hand-built problems are instant, deterministic, and have analytically computed partition functions. | No |
| D022 | M002 | convention | CPU-only default for NN training tests | NN training tests (R019, R021) run on CPU by default, GPU tests skip-marked | Reproducibility (cuDNN nondeterminism), no GPU dependency for default test path, fast execution on any machine | No |
| D023 | M002/S01 | convention | Tolerance-based convergence assertion | `final_avg < 0.9 * initial_avg` (10% decrease) not hard thresholds | Different seeds/problems may converge differently; relative decrease is robust | Yes — tighten if convergence is reliable |
| D024 | M002/S01 | convention | Direct loss function calls for edge-case tests | Call `loss_fn(outputs, targets)` directly, no Trainer wrapper | Isolates the function under test; Trainer adds noise (config, sampling, etc.) for edge-case verification | No |
| D025 | M002/S01 | convention | No-crash assertion for edge cases, not finite-output | R022/R023 assert no unhandled exception, not that output is finite | Research confirmed inf/nan outputs are expected behavior for extreme inputs; the contract is "don't crash" | Yes — if finite output becomes a requirement |
| D026 | M003 | arch | Extend DataPreprocessor with normalization_mode, not separate class | Add `normalization_mode` param ('logspace_mean' default, 'minmax_01' for NeuroBE) to DataPreprocessor | Keeps existing interface; `undo_normalization` is called in only 2 places in factor_nn.py and needs no call-site changes if mode-aware | No |
| D027 | M003 | arch | New neurobe_weighted_mse loss function, not modifying existing weighted_logspace_mse | Existing weighted_logspace_mse operates on logspace-normalized targets; NeuroBE's IS-weighted MSE operates on [0,1]-normalized targets with different weight formula. Entangling them risks breaking existing behavior. | No |
| D028 | M003 | arch | neurobe_mode as config expansion in prepare_config | `neurobe_mode: true` expands to individual settings (normalization_mode, loss_fn, lr, batch_size, etc.) in prepare_config(). Individual settings remain independently configurable for overrides. | Follows D001 pattern — config translation happens at entry point, not scattered through consumers | No |
| D029 | M003 | convention | Fold ReLU, AMP disable, hidden dim sizing into R035 neurobe_mode preset | Research identified activation (ReLU vs Tanh), AMP (NeuroBE doesn't use it), and hidden dim sizing (nArgs × var_dim vs nbe,3) as reproduction-critical. These are part of the neurobe_mode preset, not separate requirements. | Keeps requirement count focused; these are all "faithful reproduction defaults" | No |
| D030 | M003 | convention | Two-slice structure: machinery+tests first, experiments second | S01 implements normalization, early stopping, config, loss fn, activation, tests. S02 does ecl tuning, runs 15 problems, produces comparison table. | Normalization round-trip correctness must be proven before burning GPU hours on experiments | No |
| D031 | M003 | convention | ecl = 2^width_problem - 1 for binary-domain NeuroBE matching | NCE dispatches on `message_size > ecl` (strictly greater). For binary domains with NeuroBE's `_Width > width_problem`, the matching ecl is `2^width_problem - 1` so that buckets with width > width_problem have message_size > ecl. | Off-by-one analysis from research; verified against auto_ecl patterns | No |
| D032 | M003/S01 | convention | Tests-first slice verification | Create all test classes (failing) in T01 before any production code. Subsequent tasks make tests pass. | Objective stopping conditions prevent false completion; tests document the contract | No |
| D033 | M003/S01 | arch | Patience early stopping as new branch, not modifying existing | New `neurobe_early_stopping` mode alongside existing `nbe_early_stopping` — different algorithm (patience counter vs 3-consecutive-increases) | Research confirmed distinct semantics; modifying existing mode risks breaking current behavior | No |
| D034 | M003/S01 | arch | neurobe_mode defaults as "set if absent" pattern | `prepare_config()` iterates NEUROBE_DEFAULTS dict and sets keys only if not already present in flat config | User overrides must win; follows D001 pattern of config translation at entry point | No |
| D035 | M003/S02 | arch | Root-variable fix: prefer elim_order over .vo file | In `_load_from_uai`, when `elim_order` is provided, don't pass `order_file` to `uai_to_GM` — use the elim_order path which includes all n vars | `.vo` files use `skip_first=True` dropping root variable; `model.order` from `.ord.elim` has all n vars. Fixing at call site avoids modifying pyGMs. | No |
| D036 | M003/S02 | convention | Script-based verification for S02, not pytest | S02 uses standalone scripts for verification, not new pytest tests | Deliverables are experiment results and comparison tables — one-time assertions, not reusable library contracts. Existing 134 pytest tests verify all library behavior. | Yes |
| D037 | M003/S02 | arch | NEUROBE_DEFAULTS must include all Trainer-required keys | Added `approximation_method`, `debug`, `traced_losses`, `optimizer` to NEUROBE_DEFAULTS | Trainer.__init__ accesses these with bracket notation (no default); neurobe_mode configs must be self-sufficient without callers adding boilerplate | No |
