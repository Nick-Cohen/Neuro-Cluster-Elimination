I want to reduce the difficulty of turning my experiment designs into configs, from a 7 out of 10 to a 1 out of 10. You will implement changes and, when ready for me to test, notify me. Once done, you’ll send a single Discord message. This message will contain: the file location of instructions that explain exactly how to build a config and what each item means, the final example config, the outputs from the example runs, and the results. The example config will include two experiments: one is a 1-minute test, batch size 2048, in NeuroBE mode with no early stopping; the other uses bw iB2 set to 10 with unnormalized KL divergence loss. The message will also contain the file paths of the outputs generated by these example configs. Your work is complete when you believe the difficulty has dropped, and I can test it.